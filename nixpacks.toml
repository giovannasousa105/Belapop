[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo \"skip build\""]

[start]
cmd = "uvicorn skin_ai_app:app --host 0.0.0.0 --port $PORT"
